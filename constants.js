/**
 * All constant values that our sever needs
 */

const taxRate = 5; //5%
const discountPercentage = 10; //10%
const loyalityRate = 2; //2 points per $1

module.exports = { taxRate, discountPercentage, loyalityRate };
